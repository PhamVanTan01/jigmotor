# Motor 1807 firmware archive

Each subdirectory stores an immutable firmware snapshot for motor 1807.
The directory name includes the test profile and source commit so that an
older HEX file can be selected and flashed without rebuilding the source.

Do not overwrite an existing snapshot. Create a new versioned subdirectory
for every later firmware build.
